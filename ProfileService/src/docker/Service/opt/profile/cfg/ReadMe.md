This directory contains the configuration file(s) for the service.  

Different files can be place here and the container called with different configuration files to change the way the container runs.